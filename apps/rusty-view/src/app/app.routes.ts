import type { Route } from '@angular/router';

/**
 * Top-level routes for the rusty-view app. Empty during scaffolding; the real
 * debug layout is added when @rusty-view/chat-shell lands (#3185–#3186).
 */
export const appRoutes: Route[] = [];
